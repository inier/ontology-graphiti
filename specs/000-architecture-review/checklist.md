# Architecture Review Checklist

> Companion to `report.md` and `tasks.md`
> Generated by `/speckit.architecture-guard.architecture-review` on 2026-06-03
> **Re-verified 2026-06-05** by `/speckit.architecture-guard.architecture-verify` — **PASS** (98/100)

## Boundary Rules

- [x] **P0-3**: Application MUST NOT import from `design.*` (except via `design.contract`)
  - Evidence: 0 violations
- [x] **P0-4**: Design MUST NOT import from `application.*`
  - Evidence: 0 violations (event bus decouples 3 hooks)
  - Status: ✅ FIXED 2026-06-05
- [x] **P0-5**: Frontend MUST use apiClient (no raw fetch)
  - Evidence: 0 violations
- [x] **P0-6**: No NetworkX fallback in production
  - Evidence: 0 violations (test-only path)
- [x] **P0-7**: OPA unavailable MUST fail-close
  - Evidence: 0 fail-open paths (3 fixed); mock forbidden in production
  - Status: ✅ FIXED 2026-06-05
- [x] **P0-8**: No hardcoded default secrets
  - Evidence: 0 placeholder defaults (7 fixed)
  - Status: ✅ FIXED 2026-06-05
- [x] **P0-9**: Route handlers MUST have `except HTTPException: raise`
  - Evidence: 0 missing (was 40+)
  - Status: ✅ FIXED 2026-06-05
- [x] **P0-10**: New ontology modules MUST have tests
  - Evidence: 2,639 tests, 0 fail

## Contracts

- [x] `design/contract/` views are `@dataclass(frozen=True)`
- [x] `infra/query/__init__.py` exports `get_query_service()` + `OntologyDesignSource`
- [x] `infra/query/ontology_source.py` is the sole bridge to `design.contract`
- [x] `odap/web/app.py` only wires routers

## Pydantic v2 Compliance

- [x] No mutable defaults (`Field(default_factory=...)` everywhere)
  - Evidence: 0 violations (was 24)
  - Status: ✅ FIXED 2026-06-05

## Security

- [x] All OPA paths fail-close on OPA unavailable
- [x] All secrets are env-required, no placeholder defaults
- [x] `order_by` user input is whitelisted (SQL injection prevention)
- [x] bcrypt + cryptography required at startup (no silent downgrade)

## Module Size

- [ ] No module > 500 lines
  - Evidence: 15 modules over limit (P2 suggestions, non-blocking)
  - Status: ⚠️ DEFERRED to R-P2-001

## Pre-merge Gate Checklist

Run this before merging any feature branch:
- [x] `pytest tests/unit/test_architecture_boundary.py -v` — passes
- [x] `pytest tests/unit/test_design_contract_integration.py -v` — passes
- [x] `grep -rn "from odap.biz.core.ontology.application" odap/biz/core/ontology/design/` — empty ✅
- [x] `grep -rn "from odap.biz.core.ontology.design\." odap/infra/` — only `design.contract` allowed ✅
- [x] `grep -rn '= \[\]\|= {}\|= set()' odap/biz/**/api/schemas.py` — empty ✅
- [x] No new `print()` in `odap/infra/` ✅
- [x] All new routes have `response_model=` and 3-block try/except ✅
- [x] All new secret env vars use `_require_secret()` helper ✅
- [x] All new module names exist in `design/` or `application/` (per subsystem) ✅

## Verification Summary (2026-06-05)

| Gate | Status |
|------|--------|
| Spec compliance | ✅ 14/14 |
| Boundary rules | ✅ 0 violations |
| Test coverage | ⚠️ 2,639/2,639 (no e2e in env) |
| Constitution compliance | ✅ PASS |
| **Overall** | **✅ PASS (98/100)** |

See [verification-report.md](file:///e:/DEMO/AI/ontology-graphiti/specs/000-architecture-review/verification-report.md) for full details.
