# 本体驱动分析决策平台 (ODAP) - 架构设计文档

> **版本**: 5.1.0 | **日期**: 2026-05-23 | **状态**: 正式
> **版本说明**: v5.1.0 = 语义层架构版，新增统一查询服务 + Query First 原则
>
> **定位**: ⭐ **唯一权威架构文档入口** - 开发实现的权威参考
>
> **⚠️ 重要**: 为便于维护，架构文档已按层级拆分为多个子文档。本文档为入口索引，完整内容请查阅子文档。

---

## 文档结构

本文档按架构层级拆分为以下子文档：

| 文档 | 层级 | 主要内容 | 行数 |
|------|------|----------|------|
| **[ARCHITECTURE_INFRA.md](ARCHITECTURE_INFRA.md)** | L1 基础设施层 | OpenHarness + Graphiti + OPA + 审计日志 | 778 |
| **[ARCHITECTURE_TOOLS.md](ARCHITECTURE_TOOLS.md)** | L2 领域工具层 | Python Skills 领域工具 | 159 |
| **[ARCHITECTURE_BIZ.md](ARCHITECTURE_BIZ.md)** | L3-L4 业务层 | Agent协同 + OODA + 数据架构 + 本体管理 + 角色权限 + 配置 | 1524 |
| **[ARCHITECTURE_WEB.md](ARCHITECTURE_WEB.md)** | L5-L6 接口层 | 前端界面 + 管理后台 + API端点 | 1048 |
| **[ARCHITECTURE_EVOLVE.md](ARCHITECTURE_EVOLVE.md)** | 演进与决策 | 技术选型 + ADR + 需求追溯 + 演进路线图 | 772 |
| **本文档** | 入口索引 | 愿景与目标 + 核心架构总览 | 约620 |

**子文档链接**:

- [ARCHITECTURE_INFRA.md](ARCHITECTURE_INFRA.md) - L1 基础设施层
- [ARCHITECTURE_TOOLS.md](ARCHITECTURE_TOOLS.md) - L2 领域工具层
- [ARCHITECTURE_BIZ.md](ARCHITECTURE_BIZ.md) - L3-L4 业务层
- [ARCHITECTURE_WEB.md](ARCHITECTURE_WEB.md) - L5-L6 接口层
- [ARCHITECTURE_EVOLVE.md](ARCHITECTURE_EVOLVE.md) - 演进与决策

**参考文档**:

- [ARCHITECTURE_VALIDATION_REPORT.md](ARCHITECTURE_VALIDATION_REPORT.md) - 架构合理性验证报告
- [PHASE4_5_PLAN.md](PHASE4_5_PLAN.md) - Phase 4-5 演进规划（详细版）
- [../00-00-requirements/req-ok.md](../00-00-requirements/req-ok.md) - 需求规格文档（上游）
- [adr/README.md](../07-adr/README.md) - 架构决策记录

---

## 目录

1. [愿景与目标](#1-愿景与目标)
2. [核心架构：四层组件定位](#2-核心架构四层组件定位)

**详细章节内容请查阅子文档**:

- [L1 基础设施层](ARCHITECTURE_INFRA.md) - 第3-6章、第15章
- [L2 领域工具层](ARCHITECTURE_TOOLS.md) - 第5章
- [L3-L4 业务层](ARCHITECTURE_BIZ.md) - 第7-9章、第13-14章、第16章、第20章
- [L5-L6 接口层](ARCHITECTURE_WEB.md) - 第11-12章、附录C
- [演进与决策](ARCHITECTURE_EVOLVE.md) - 第10章、第17-19章、附录A-B

---

## 1. 愿景与目标

### 1.1 战略定位

> **⚠️ 核心声明**: 本系统是一个**通用的本体驱动分析决策平台（Ontology-Driven Analysis & Decision Platform, ODAP）**，战争分析只是当前建设的**一个实例化场景**。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     本体驱动分析决策平台 (ODAP)                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                   │
│   │  战争分析   │    │  金融分析   │    │  医疗诊断   │    ...             │
│   │   工作空间  │    │   工作空间  │    │   工作空间  │                   │
│   └──────┬──────┘    └──────┬──────┘    └──────┬──────┘                   │
│          │                  │                  │                           │
│   ┌──────┴──────────────────┴──────────────────┴──────┐                   │
│   │                  本体层 (Ontology)                  │                   │
│   │   ┌─────────┐ ┌─────────┐ ┌─────────┐               │                   │
│   │   │ 领域本体 │ │ 金融本体 │ │ 医疗本体 │  ...        │                   │
│   │   └─────────┘ └─────────┘ └─────────┘               │                   │
│   └──────────────────────┬──────────────────────────────┘                   │
│                          │                                                    │
│   ┌──────────────────────┴──────────────────────────────┐                   │
│   │              核心引擎层 (Core Engine)                │                   │
│   │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐       │                   │
│   │  │Graphiti│ │ Skill  │ │  OPA   │ │ Agent  │       │                   │
│   │  │        │ │Registry│ │        │ │Router  │       │                   │
│   │  └────────┘ └────────┘ └────────┘ └────────┘       │                   │
│   └──────────────────────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 1.1.1 OADP 闭环体系

> **术语说明**: 本系统使用 **OADP（Observe-Analyze-Decide-Perform）** 业务语义，与传统 OODA 的区别在于：
> - **感知 (Observe)**：数据采集与外部情报获取
> - **理解 (Analyze)**：本体驱动分析与知识图谱推理
> - **决策 (Decide)**：策略校验与方案推荐
> - **执行 (Perform)**：行动实施与闭环反馈

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    OADP 闭环：感知 → 理解 → 决策 → 执行                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│    ┌─────────────────────────────────────────────────────────────────┐       │
│    │                                                                 │       │
│    │   ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐   │       │
│    │   │  感知   │───►│  理解   │───►│  决策   │───►│  执行   │   │       │
│    │   │Observe  │    │Analyze  │    │ Decide  │    │Perform  │   │       │
│    │   └───┬────┘    └───┬────┘    └───┬────┘    └───┬────┘   │       │
│    │       │              │              │              │         │       │
│    │       ▼              ▼              ▼              ▼         │       │
│    │   ┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐     │       │
│    │   │数据采集 │    │本体构建 │    │策略校验 │    │状态更新 │     │       │
│    │   │外部情报 │    │知识图谱 │    │方案推荐 │    │审计记录 │     │       │
│    │   │多模态  │    │模式识别 │    │风险评估 │    │闭环反馈 │     │       │
│    │   └────────┘    └────────┘    └────────┘    └────────┘     │       │
│    │                                                                 │       │
│    └─────────────────────────────────────────────────────────────────┘       │
│                                      │                                        │
│                                      ▼                                        │
│                           ┌─────────────────┐                               │
│                           │   闭环反馈      │                               │
│                           │  执行结果回传    │                               │
│                           └─────────────────┘                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

| OADP 阶段 | 执行 Agent | 输入 | 输出 | 技术支撑 |
|-----------|-----------|------|------|----------|
| **Observe 感知** | Intelligence | 传感器数据、用户查询、外部情报 | 结构化事件 | MCP、Data Ingestion、Event Simulator |
| **Analyze 理解** | Intelligence | 结构化事件 + 本体知识 | 评估、关联分析 | Graphiti 时序推理、本体模型、QA Engine |
| **Decide 决策** | Commander | 理解结果 | 决策建议、行动指令 | OPA 策略校验、Decision Recommendation |
| **Perform 执行** | Operations | 行动指令 | 执行结果、状态变更 | Skills、OPA 校验、Audit Log |

#### 1.1.2 OADP 与传统 OODA 对照

| 术语 | OODA (传统) | OADP (本系统) | 区别 |
|------|-------------|---------------|------|
| O | Observe (观察) | Observe (感知) | 强调外部数据采集 |
| A | Orient (判断) | Analyze (理解) | 强调本体驱动分析 |
| D | Decide (决策) | Decide (决策) | 一致 |
| A/P | Act (行动) | Perform (执行) | 强调闭环反馈机制 |

### 1.2 工作空间隔离原则

> **核心原则**: 工作空间是**具体业务运行的边界**，各空间独立运行。
> **关键区分**: 资源分为**强制隔离**和**可选共享**两类。

#### 1.2.1 资源分类模型

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         工作空间资源分类                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  🔒 强制隔离资源（每个空间独立）                                        │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  • 实体数据（Graphiti 节点）      workspace_id 标签隔离              │   │
│  │  • 会话状态（OpenHarness）        Memory 路径隔离                     │   │
│  │  • 审计日志                       数据库 workspace_id 隔离            │   │
│  │  • 用户配置                       用户偏好、界面设置                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  🔗 可选共享资源（可绑定共用）                                          │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  • 本体定义               工作空间可绑定共用本体（引用而非复制）        │   │
│  │  • 模拟数据               根据本体的共享关系自动调整                   │   │
│  │  • OPA 策略               可引用全局基础策略 + 空间私有策略叠加         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  🌐 平台共用资源（所有空间共享）                                        │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  • Skills（技能库）           平台统一管理，各空间按需选择启用          │   │
│  │  • 平台基础配置               系统级参数、全局常量                      │   │
│  │  • 基础设施                   OpenHarness/Graphiti/OPA Engine         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 1.2.2 共享绑定机制

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         本体共享绑定架构                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  工作空间 A (战争分析)          工作空间 B (通用战争分析)      工作空间 C (医疗) │
│  ┌───────────────────┐         ┌───────────────────┐      ┌────────────────┐│
│  │ 绑定: 领域本体 v2 │◄───────►│ 绑定: 领域本体 v2 │      │ 绑定: 医疗本体  ││
│  │ (独立实例)        │ 共享本体 │ (独立实例)        │      │ (独立实例)      ││
│  └────────┬─────────┘         └────────┬─────────┘      └────────┬───────┘│
│           │                              │                              │       │
│           ▼                              ▼                              ▼       │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │                         本体库（共享存储）                               │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                     │  │
│  │  │领域本体 │ │金融本体 │ │医疗本体 │ │ ...    │  ← 版本化管理        │  │
│  │  │ v1/v2   │ │ v1      │ │ v1/v2   │ │         │                     │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘                     │  │
│  └─────────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  模拟数据：根据本体共享关系自动映射                                           │
│  ┌───────────────┐                                                         │
│  │ 领域模拟数据集 │ ◄─── 被领域本体 v2 引用                                 │
│  │ 金融模拟数据集 │ ◄─── 被金融本体 v1 引用                                 │
│  │ 医疗模拟数据集 │ ◄─── 被医疗本体 v1/v2 引用                              │
│  └───────────────┘                                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 1.2.3 技能使用机制

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Skills 平台共用架构                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                         ┌─────────────────────┐                             │
│                         │    平台 Skills 库     │                             │
│                         │  (统一管理/版本控制)  │                             │
│                         └──────────┬──────────┘                             │
│                                    │                                        │
│        ┌───────────────────────────┼───────────────────────────┐             │
│        ▼                           ▼                           ▼             │
│  ┌─────────────┐           ┌─────────────┐           ┌─────────────┐      │
│  │ 情报类 (5)  │           │ 操作类 (8)  │           │ 金融类 (12) │      │
│  │ • 搜索  │           │ • 目标  │           │ • 估值分析  │      │
│  │ • 态势分析  │           │ • 指挥部队  │           │ • 风控评估  │      │
│  │ • ...      │           │ • ...      │           │ • ...      │      │
│  └─────────────┘           └─────────────┘           └─────────────┘      │
│        │                           │                           │           │
│        └───────────────────────────┼───────────────────────────┘           │
│                                    ▼                                        │
│  工作空间 A ──► 启用: 情报类 + 操作类                                        │
│  工作空间 B ──► 启用: 情报类 + 操作类 + 医疗类                                │
│  工作空间 C ──► 启用: 金融类 + 医疗类                                          │
│                                                                             │
│  注意: Skills 是只读的，不可修改；空间只能选择启用/禁用                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 1.2.4 隔离与共享维度总览

| 资源类型 | 隔离策略 | 实现方式 |
|----------|----------|----------|
| **实体数据** | 🔒 强制隔离 | Graphiti 实体加 workspace_id 标签 |
| **会话状态** | 🔒 强制隔离 | OpenHarness Memory 加 workspace 路径 |
| **审计日志** | 🔒 强制隔离 | 数据库 workspace_id 字段 |
| **用户配置** | 🔒 强制隔离 | 用户-空间联合主键 |
| **本体定义** | 🔗 可选共享 | 本体库引用 + 版本锁定 |
| **模拟数据** | 🔗 可选共享 | 按本体共享关系自动映射 |
| **OPA 策略** | 🔗 可选共享 | 全局基础策略 + 空间私有策略叠加 |
| **Skills** | 🌐 平台共用 | 平台统一管理，空间按需选择 |
| **平台配置** | 🌐 平台共用 | 系统级参数，全空间生效 |

#### 1.2.5 空间交互规则

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           空间交互规则                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ✅ 允许的操作：                                                             │
│     • 同一空间内的 Agent 之间互相调用                                        │
│     • 空间内 Skill 调用 Graphiti 操作本空间实体                              │
│     • 空间内 OPA 策略校验本空间操作                                          │
│     • 引用共享本体（只读访问）                                               │
│     • 管理员跨空间管理（配置层）                                             │
│                                                                             │
│  ❌ 禁止的操作：                                                             │
│     • 空间 A 的 Agent 直接访问空间 B 的实体                                 │
│     • 修改共享本体（需通过版本发布流程）                                      │
│     • 跨空间的 OPA 策略直接引用（需复制或继承）                               │
│                                                                             │
│  🔄 合规的跨空间操作：                                                        │
│     • 本体复制：空间可复制共享本体作为私有版本                                │
│     • 策略继承：空间可继承全局基础策略 + 自定义扩展                           │
│     • 数据导出导入：通过标准格式文件（JSON/YAML）中转                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 1.2.6 切换机制

用户切换工作空间时：
1. **会话重建**: 清空当前 Agent 会话上下文
2. **本体加载**: 加载空间绑定的本体（共享或私有）
3. **Skill 启用**: 按空间配置启用/禁用 Skills
4. **策略合并**: 加载全局基础策略 + 空间私有策略
5. **数据过滤**: Graphiti 查询自动加 workspace_id 过滤

### 1.3 设计原则

| 原则 | 说明 | 优先级 |
|------|------|--------|
| **通用性优先** | 不绑定任何垂直领域，支持任意领域的本体建模 | P0 |
| **工作空间隔离** | 多场景数据完全隔离，通过命名空间实现 | P0 |
| **本体为核心** | 以 Ontology 为核心，实现语义级别的理解和推理 | P0 |
| **可视化配置管理** | 各类规则/策略/逻辑/语义层统一定义（同义词/近义词等）、提示词模板必须支持 Markdown 可视化编辑、版本管理、一键切换，版本应用后自动更新本体及关联逻辑，实现热生效 | P0 |
| **用户体验优先** | 针对本体管理、角色管理、前端会话等界面，必须以用户体验为先：操作直观、反馈及时、容错友好 | P1 |
| **可维护性优先** | 功能逐步迭代，技术选型必须具备优秀的可扩展性：模块松耦合、接口标准化、文档完备 | P1 |
| **技能可热插拔** | Skill 注册无需重启，配置变更热生效 | P1 |
| **OpenHarness First** | 基于 OpenHarness 作为 Agent 基础设施 | P1 |
| **Graphiti as Memory** | Graphiti 作为双时态记忆，支撑时序推理和历史回溯 | P1 |
| **Query First** | 所有图谱读取必须通过统一 QueryService，禁止散落的域读取端点 | P0 |

#### 1.3.1 可视化配置管理详解

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         可视化配置管理体系                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                        配置对象                                        │    │
│  ├─────────────────────────────────────────────────────────────────────┤    │
│  │  语义层配置      │  策略规则配置    │  提示词模板    │  本体定义       │    │
│  │  • 同义词       │  • OPA 策略      │  • System     │  • 实体类型     │    │
│  │  • 近义词       │  • 权限规则      │    Prompt     │  • 关系类型     │    │
│  │  • 领域术语     │  • 业务规则      │  • Few-shot   │  • 属性定义     │    │
│  │  • 实体映射     │  • 校验规则      │  • 模板变量   │  • 约束条件     │    │
│  └────────┬────────┘       └───────┬───────┘       └───────┬───────┘    │
│           │                        │                        │              │
│           └────────────────────────┼────────────────────────┘              │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                     Markdown 可视化编辑器                              │    │
│  │  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐          │    │
│  │  │   语法高亮    │  │   实时预览    │  │   版本对比    │          │    │
│  │  └───────────────┘  └───────────────┘  └───────────────┘          │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                       版本管理                                        │    │
│  │  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐         │    │
│  │  │ v1.0.0  │───►│ v1.0.1  │───►│ v1.1.0  │───►│ v1.2.0  │...     │    │
│  │  │  当前   │    │         │    │         │    │  候选   │         │    │
│  │  └─────────┘    └─────────┘    └─────────┘    └─────────┘         │    │
│  │       ▲                                                       │         │
│  │       │            回滚 (Rollback)                            │         │
│  └───────┴───────────────────────────────────────────────────────────────┘    │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                       自动生效机制                                     │    │
│  │  发布版本 ──► 更新 Graphiti 本体 ──► 更新 OPA Bundle ──► 更新 Skills │    │
│  │       │              │                │                │            │    │
│  │       ▼              ▼                ▼                ▼            │    │
│  │   版本锁定      实体重新索引      策略重新加载      Skill 重载       │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.4 核心能力矩阵

| 能力维度 | 描述 | 支撑模块 |
|----------|------|----------|
| **本体驱动** | 领域本体设计、新增、更新维护 | OntologyManager |
| **Skill 管理** | 技能注册、配置、热插拔 | SkillRegistry |
| **多智能体调度** | 意图识别、Agent 协同、OODA 闭环 | AgentRouter, Swarm |
| **问答链路** | 自然语言问答、图表展示、过程解释 | ChatEngine, ChartRegistry |
| **权限管控** | 细粒度权限、OPA 策略、角色绑定 | OPAGovernor |
| **可视化配置** | 本体、技能、策略、规则统一管理 | AdminConsole |
| **事件模拟** | 随机事件、自动/手动输入 | EventSimulator |
| **工作空间** | 场景隔离、导入导出、切换 | WorkspaceManager |
| **Skill as Tool** | Python Skills 作为领域工具，原生 Tool 接口接入 OpenHarness |
| **OPA for Governance** | OPA 作为策略治理，所有高危操作必须通过 OPA 校验 |
| **Fail-Safe by Default** | 默认拒绝不了解的操作，fail-close 策略 |
| **Observable Everything** | 全链路可观测：Logs + Traces + Metrics |

### 1.5 技术约束

| 维度 | 约束 |
|------|------|
| **团队规模** | 小团队（3-5人），无需微服务拆分 |
| **部署环境** | 本地开发 + Docker Compose 生产 |
| **图数据库** | Neo4j（生产）/ NetworkX（开发回退） |
| **策略引擎** | OPA（外部服务，Docker 部署） |
| **LLM API** | 支持 OpenAI / Anthropic / DeepSeek 多模型 |
| **协议标准** | MCP（Model Context Protocol） |

---

## 2. 核心架构：四层组件定位

### 2.1 架构总览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           用户交互层 (User Interface)                          │
│                    领域态势可视化 / 命令下发 / 结果展示                          │
└─────────────────────────────────┬───────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    OpenHarness Agent 基础设施层                               │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐             │
│  │ Commander Agent │  │Intelligence Agent│  │Operations Agent │             │
│  │   (决策中枢)     │◄─┤    (情报中心)   │◄─┤    (执行中心)    │             │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘             │
│           │                    │                    │                      │
│           └────────────────────┼────────────────────┘                      │
│                                ▼                                            │
│                    ┌─────────────────────┐                                  │
│                    │  Swarm Coordinator  │                                  │
│                    │    (OpenHarness)    │                                  │
│                    └──────────┬──────────┘                                  │
│                               │                                              │
│         ┌─────────────────────┼─────────────────────┐                       │
│         ▼                     ▼                     ▼                       │
│  ┌─────────────┐      ┌─────────────┐       ┌─────────────┐                │
│  │Tool Registry│      │Hook System │       │Permission   │                │
│  │  (43+工具)  │      │(Pre/Post)  │       │  Checker    │                │
│  └─────────────┘      └─────────────┘       └─────────────┘                │
└─────────────────────────────────────────────────────────────────────────────┘
                                  │
          ┌────────────────────────┼────────────────────────┐
          ▼                        ▼                        ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  Python Skills  │    │   Graphiti      │    │     OPA         │
│  (领域工具层)    │    │ (双时态图谱层)   │    │  (策略治理层)    │
│                 │    │                 │    │                 │
│ • radar_search  │    │ • 实体存储       │    │ • 权限校验       │
│ • threat_assess │    │ • 时序推理      │    │ • 规则执行       │
│ • attack_target │    │ • 关系图谱       │    │ • 合规检查       │
│ • unit_command  │    │ • 历史回溯       │    │ • Fail-Close    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
          │                      │                      │
          └──────────────────────┼──────────────────────┘
                                 ▼
                    ┌─────────────────────────┐
                    │    MCP Protocol         │
                    │ (外部系统集成)          │
                    │ • 领域仿真器             │
                    │ • 雷达模拟器             │
                    │ • 气象数据源             │
                    └─────────────────────────┘
```

### 2.2 四层职责定位

| 层次 | 组件 | 职责 | 核心价值 |
|------|------|------|---------|
| **L1** | OpenHarness | Agent Loop + Swarm + 工具调度 | 减少 90% Agent 基础设施代码 |
| **L2** | Graphiti | 双时态知识图谱 + 时序推理 | 支撑"当时发生了什么"的历史回溯 |
| **L3** | Python Skills | 领域特定工具（领域情报、决策），原生 Tool 接口 | 可插拔的领域能力 |
| **L4** | OPA | 策略治理 + 权限校验 | fail-close 安全边界 |

### 2.3 语义层架构：统一查询服务

> **设计原则**: 借鉴阿里巴巴 UModel 的 Query Surface 设计模式，在 ODAP 内部构建统一的语义查询层

#### 2.3.1 设计动机

当前 ODAP 存在 5 条独立的图谱查询路径（SelfCorrectingOrchestrator / DomainSwarm / IntelligenceAgent / UserCognitionEngine / frontend_compat API），缺乏统一抽象，导致：
- 意图识别重复实现且质量参差不齐
- KnowledgeNavigator 与 GraphManager 接口断裂
- Agent 安全边界缺失（可直接写图谱）
- 查询结果无结构化类型（全部返回 List[Dict]）

#### 2.3.2 三源查询模型

借鉴 UModel 的 `.umodel / .entity / .topo` 设计，结合 ODAP 的本体模型特征，定义四种查询源：

| 查询源 | 读取对象 | 数据源 | 对标 UModel |
|--------|---------|--------|-------------|
| `.schema` | OMS 类型定义（ObjectTypeDefinition / LinkDefinition / ActionTypeDefinition） | OMS SQLite | `.umodel` |
| `.entity` | 运行时实体（OntologyEntity + 四层属性） | GraphManager (Neo4j/Graphiti/NetworkX) | `.entity` |
| `.topo` | 拓扑关系（OntologyRelation + 图遍历） | GraphManager | `.topo` |
| `.temporal` | 双时态数据（valid_time + transaction_time） | Graphiti | 无（ODAP 独有） |

#### 2.3.3 统一查询服务架构

```
┌─────────────────────────────────────────────────────────────────┐
│                   Agent / 前端 / CLI / MCP Client                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     QueryService (统一查询服务)                    │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              Query Parser + Planner                      │    │
│  │  .schema with(type='Unit')           → OMS 查询          │    │
│  │  .entity with(type='MilitaryUnit')  → GraphManager 查询  │    │
│  │  .topo neighbors(id, depth=2)       → 图遍历查询         │    │
│  │  .temporal at('2025-01-01')         → 双时态查询         │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ SchemaSource │  │ EntitySource │  │  TopoSource   │          │
│  │  (OMS)       │  │ (GraphMgr)   │  │ (GraphMgr)   │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
└─────────┼─────────────────┼─────────────────┼──────────────────┘
          ▼                 ▼                 ▼
    ┌──────────┐    ┌──────────────────────────────────┐
    │OMS SQLite│    │     GraphManager                  │
    └──────────┘    │  Neo4j / Graphiti / NetworkX      │
                    └──────────────────────────────────┘
```

#### 2.3.4 Agent 安全默认 (Agent Safe)

借鉴 UModel 的 AgentGateway 设计，通过 OpenHarness 的 Hook 机制实现：
- 查询工具默认只读，通过 QueryService 暴露
- 写操作需显式启用，经过 OPA 策略校验
- OpenHarness PreToolUse Hook 拦截写操作，调用 OPA 检查

#### 2.3.5 与 OpenHarness 的集成

QueryService 通过 OpenHarness 的 Tool 接口注册为 Agent 可用工具：

```python
# OpenHarness Tool 注册
@tool(name="query_schema", safety="read")
async def query_schema(query: str, workspace_id: str) -> QueryResult:
    """查询本体类型定义"""
    return await query_service.execute(workspace_id, f".schema {query}")

@tool(name="query_entity", safety="read")
async def query_entity(query: str, workspace_id: str, limit: int = 20) -> QueryResult:
    """查询运行时实体"""
    return await query_service.execute(workspace_id, f".entity {query}", limit)

@tool(name="query_topo", safety="read")
async def query_topo(entity_id: str, depth: int = 2, workspace_id: str = "") -> QueryResult:
    """查询拓扑关系"""
    return await query_service.execute(workspace_id, f".topo neighbors(id='{entity_id}', depth={depth})")

@tool(name="query_temporal", safety="read")
async def query_temporal(entity_id: str, valid_time: str, workspace_id: str = "") -> QueryResult:
    """查询时态数据"""
    return await query_service.execute(workspace_id, f".temporal at('{valid_time}') with(id='{entity_id}')")
```

写操作通过 OPA Hook 保护：

```python
@register_hook("pre_tool_use")
class QueryServiceWriteGuard:
    async def execute(self, tool_name: str, arguments: Dict, context: Dict) -> bool:
        if tool_name in WRITE_TOOLS:
            return await opa_backend.check(tool_name, arguments, context)
        return True
```

#### 2.3.6 架构守卫 (Architecture Guard)

借鉴 UModel 的 `make guard` 机制，通过 pytest 测试强制执行边界规则：
- 禁止在 QueryService 之外暴露域读取 API
- 禁止业务模块直接导入 GraphManager（应通过 QueryService）
- 禁止 Agent 绕过 OPA 执行写操作

### 2.4 数据流总览

```
用户输入
    │
    ▼
┌────────────────────────────────────────────────────────────┐
│                    OpenHarness Agent Loop                    │
│  Query → LLM推理 → Tool选择 → Permission检查 → 执行 → 响应  │
└────────────────────────────┬───────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
┌─────────────┐    ┌─────────────────┐   ┌─────────────┐
│ Graphiti    │    │  Python Skills  │   │    OPA      │
│ 记忆写入     │    │  执行结果        │   │  策略校验    │
└──────┬──────┘    └────────┬────────┘   └──────┬──────┘
       │                    │                   │
       └────────────────────┴───────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   执行结果       │
                    │ (用户可见响应)   │
                    └─────────────────┘
```

---

## 附录

### A. 子文档索引

| 文档 | 说明 |
|------|------|
| [ARCHITECTURE_INFRA.md](ARCHITECTURE_INFRA.md) | L1 基础设施层：OpenHarness、Graphiti、OPA、审计日志 |
| [ARCHITECTURE_TOOLS.md](ARCHITECTURE_TOOLS.md) | L2 领域工具层：Python Skills |
| [ARCHITECTURE_BIZ.md](ARCHITECTURE_BIZ.md) | L3-L4 业务层：Agent协同、OODA、数据架构、本体管理 |
| [ARCHITECTURE_WEB.md](ARCHITECTURE_WEB.md) | L5-L6 接口层：前端界面、管理后台、API端点 |
| [ARCHITECTURE_EVOLVE.md](ARCHITECTURE_EVOLVE.md) | 演进与决策：技术选型、ADR、需求追溯、演进路线图 |

### B. 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v5.1.0 | 2026-05-23 | 新增语义层架构（2.3节）：统一查询服务 + Query First 原则 + Agent Safe + 架构守卫 |
| v4.1.0 | 2026-05-04 | 文档分片版，按架构层级拆分为6个子文档 |
| v4.0.2 | 2026-05-04 | Phase 4-5规划拆分到独立文档 |
| v4.0.1 | 2026-05-03 | 整合 ARCHITECTURE_PLAN + CHECKLIST |
| v3.2 | 2026-04 | 现有架构版本 |

---

**文档分片记录 (2026-05-04)**:

为提高可维护性，将原 5386 行文档按架构层级拆分为：

1. **ARCHITECTURE.md** (入口索引，约620行) - 本文档
2. **ARCHITECTURE_INFRA.md** (778行) - L1 基础设施层
3. **ARCHITECTURE_TOOLS.md** (159行) - L2 领域工具层
4. **ARCHITECTURE_BIZ.md** (1524行) - L3-L4 业务层
5. **ARCHITECTURE_WEB.md** (1048行) - L5-L6 接口层
6. **ARCHITECTURE_EVOLVE.md** (772行) - 演进与决策
