"""Inheritance 模块 — ObjectType 继承 + Mixin (Phase 11 M2 FR-033)

对齐 AGENTS.md 规范：
- 服务层不抛 HTTPException
- Enum 必须 (str, Enum) 双继承
- 容器字段必须 Field(default_factory=...)
- 调用链 routes → services → impl → storage
"""
